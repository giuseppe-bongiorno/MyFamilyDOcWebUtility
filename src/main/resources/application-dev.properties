spring.application.name=MyFamilyDocWebUtility
server.port=8082
# ==================================
# TELEGRAM SERVICE CONFIGURATION
# ==================================

# Telegram Bot API Configuration
telegram.api.base-url=https://api.telegram.org
telegram.api.timeout=30000

# Connection pool for RestTemplate (optional optimization)
http.client.connection-timeout=5000
http.client.read-timeout=30000
http.client.max-connections=100

# ==================================
# DATABASE CONFIGURATION
# ==================================

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://57.129.16.202:5432/auth_otp_db
spring.datasource.username=admin
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.password=Oriolo,1984

# Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_schema=web_utility



jwt.secret=2d9Xeac5705c1d5f659104b74f5dad0be7cY0f20cdf8eV6f1ef2afd58dec54af30cc7d79f140671eccec937a21d1a279Z3c1e2c3b94b9c43b3Q3a2c43bb8bec5
jwt.expiration=86400000
# ==================================
# LOGGING CONFIGURATION
# ==================================

# ============================================
# Logging
# ============================================
logging.level.root=INFO
logging.level.it.myfamilydoc.webutility=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Log pattern
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ============================================
# Actuator Endpoints (Health checks)
# ============================================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.health.db.enabled=true

# ============================================
# Server Configuration
# ============================================
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain

# Error handling
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
server.error.include-exception=false

# ============================================
# Spring Boot DevTools (solo in development)
# ============================================
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# ============================================
# Telegram Bot Configuration (opzionale)
# Se vuoi salvare configurazioni bot predefinite
# ============================================
# telegram.default.bot-token=123456:ABC-DEF...
# telegram.default.chat-id=12345678
